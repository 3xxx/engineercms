function AssignBlock() {
    var database = mxOcx.GetDatabase();
    if (database.GetBlockTable().Has("myblocktable"))
    {
        // 已经插入.
        alert("true");
    }else {
        alert('false');
    }
}